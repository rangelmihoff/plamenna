import { useQuery } from '../contexts/QueryProvider';

function ProductsList() {
  const { query } = useQuery();
  const [products, setProducts] = useState([]);

  useEffect(() => {
    const loadProducts = async () => {
      const data = await query('/api/products');
      setProducts(data);
    };
    loadProducts();
  }, []);

  // ...
}