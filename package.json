{
  "name": "shopify-ai-seo-20",
  "version": "2.0.0",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run frontend:dev\"",
    "start": "node server.js",
    "server:dev": "nodemon server.js",
    "frontend:dev": "cd frontend && npm start",
    "build": "npm run build:frontend && npm run copy:build",
    "build:frontend": "cd frontend && npm run build",
    "copy:build": "cp -r frontend/build/* public/ && echo 'Frontend build copied to public/'",
    "setup": "npm install && cd frontend && npm install",
    "railway:build": "npm run setup && npm run build",
    "test": "jest",
    "clean": "rm -rf public/* frontend/build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  }
}